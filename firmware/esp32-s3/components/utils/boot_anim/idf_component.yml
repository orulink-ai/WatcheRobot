version: "1.0.0"
description: "Util: boot_anim"
url: "https://github.com/Ro-In-AI/WatcheRobot-Firmware"
license: "Apache-2.0"
targets:
  - esp32s3
dependencies:
  idf: ">=5.2"
  # LVGL dependencies are provided by main/idf_component.yml
  # Local components (sensecap-watcher, anim_service) are resolved via CMakeLists.txt
