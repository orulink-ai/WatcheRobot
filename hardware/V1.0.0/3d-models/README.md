# 3D Models

Use `source/` for editable model sources and `exports/` for public export formats such as STEP, STL, 3MF, OBJ, or GLB.

If a file is too large for Git, upload it to the matching GitHub Release and record its filename, purpose, SHA256, and download URL here.
